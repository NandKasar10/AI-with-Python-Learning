# mutable example

spice_mix = set()
# sets represents collection of objects 

print(f"Initial spice mix id : {id(spice_mix)}")

spice_mix.add("Ginger")
spice_mix.add("cardamom")

print(f"Initial spice mix id : {id(spice_mix)}")

print(spice_mix)